export enum CallType {
    outgoing = 'outgoing',
    incoming = 'incoming',
    pozvonim = 'pozvonim',
}

export enum Disposition {
    failed = 'FAILED',
    answered = 'ANSWERED',
    busy = 'BUSY',
    noAnswer = 'NO ANSWER',
}
