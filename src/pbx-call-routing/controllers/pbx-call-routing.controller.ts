import { Body, Controller, Get, HttpException, HttpStatus, Req, Res } from '@nestjs/common';
import { Response, Request } from 'express';

@Controller('pbx-call-routing')
export class PbxCallRoutingController {}
